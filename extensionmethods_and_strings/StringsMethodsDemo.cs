// strings are immutable so that even if we concat the string using + we end up creating a new string and a new memory place for a string

class StringsMethodsDemo
{
    static void Main(){
        string s = "sanjay";
        Console.WriteLine(s);
        
    }
}