//  there are many collections that are present and these are introduced from the early version itself
//  stack,queue,linkedlist,sortedlist,arraylist,hashable these are called as nongeneric collections 
// the main and beautiful thing that array list have is auto resizing the space 
// we can also give the initial value size and it will again resize to 2 times afer completing the space 
using System.Collections;
class ArrayLists
{
    // static void Main(){
        
    //     ArrayList al = new ArrayList();
    //     al.Add("sanjay");al.Add("madhu");al.Add("kamala");al.Add("jennny");al.Add("bhavs");al.Add("mallesh");
    //     Console.WriteLine(al.Capacity); // arraylist stores the values by allocating first 4 values next 8 values next 16 values like that 
    //     ArrayList al1 = new ArrayList(10); // here we can give the size of the array list at initilization and it doubles if size is completed or occupied
    //     Console.WriteLine(al.Capacity);
    //     foreach (var item in al)
    //     {
    //         Console.WriteLine(item);
    //     }
    //     al.Insert(6,"nims");
    //     foreach (var item in al)
    //     {
    //         Console.WriteLine(item);
    //     }
    //     al.Remove("nims");
    //     foreach (var item in al)
    //     {
    //         Console.WriteLine(item);
    //     }
        
    //     Array ar = new Array[10]; // here is how we declare a arraylist
    //     // there are many major differences between the array and the arraylist and some of them are arrays are unmutable i.e 
    //     // we cannot add or remove the values so the size of array cannot be changed as well after once declared 
    //     Console.WriteLine(ar.Length);

    // }


}
