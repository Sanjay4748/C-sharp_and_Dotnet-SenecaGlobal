// dictionarys are used for storing the key value pairs in them 
class Dictionary
{
    // static void Main(){

    //     Dictionary<string,object> dictionary = new Dictionary<string,object>(); // we can create a dict like this as well and also hashtable 
    //     dictionary.Add("name","sanjay");
    //     dictionary.Add("age",20);
    //     dictionary.Add("technology","python");
    //     dictionary.Add("experience",2.5f);

    //     // main difference between the dictionary and the hashtable is that hashtable stores the values in hashcode sequence or order
    //     // where as dictionary stores the values in the order which we gave 
    //     foreach(var key in dictionary.Keys)
    //     Console.WriteLine(key);

    //     foreach(var value in dictionary.Values)
    //     Console.WriteLine(value);

    // }
}