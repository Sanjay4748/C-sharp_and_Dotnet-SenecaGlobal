// generic collections are something that are type safe as well as size increasable 
// basicallys arrays are type safe but not size changable
// and collections like arraylist are size changable but not type size so thats y generic collections collections comes into playass
using System.ComponentModel;

class GenericCollection
{
   
    // static void Main(){
    //     List<int> li = [20, 24, 23, 13, 29]; // we can declare the list like this of int values
    //     List<int> li1 = new List<int>(); // we can also declare the values like this also 
    //     li1.Add(10);
    //     li1.Add(20);
    //     li1.Add(30);
    //     li1.Add(40);
    //     li1.Insert(2,25);
    //     GenericCollection gc = new GenericCollection();
        
    // }
}